#Author: jaganathan
name: Demo
on:
  pull_request:
    types:
      - opened
      - synchronize
 
jobs:
  build:
    runs-on:
      - self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Run a script
        run: |
          echo Hello, World!
          sleep 15
          echo Goodbye!
